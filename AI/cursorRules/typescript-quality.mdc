---
description: TypeScript/JavaScript 代码质量与最佳实践
globs: ["**/*.ts", "**/*.tsx", "**/*.js", "**/*.jsx", "**/*.vue"]
alwaysApply: false
---

# TypeScript / JavaScript 代码质量

以资深全栈开发标准编写高质量、易维护的代码。**少即是多**，代码行数即债务。

## 核心原则

- **简洁**：简单直接的代码
- **可读**：易于阅读和理解
- **性能**：兼顾性能，不牺牲可读性
- **可维护**：易于维护和扩展
- **可测试**：便于单测和集成
- **可复用**：组件与函数可复用

## 实践要点

1. **Early Return**：提前返回，减少嵌套
2. **条件 class**：class 条件优先用数组/模板，少用三元
3. **描述性命名**：事件处理加 `handle` 前缀（`handleClick`、`handleKeyDown`）
4. **常量优于函数**：能用 const 就用 const
5. **DRY**：避免重复，抽取公共逻辑
6. **函数式与不可变**：尽量采用函数式、不可变写法
7. **最小改动**：只改与任务相关的部分

## 注释与文档

- 每个函数开头加简要注释说明用途
- TypeScript 用类型替代 JSDoc；纯 JS 用 JSDoc

## 函数顺序

- 组合/聚合其他函数的函数放在前面

## 遇到问题

- 发现 bug 或指令会导致问题，用 `TODO:` 注释标注
