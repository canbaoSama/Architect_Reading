---
description: 核心编码规范 - DRY、SOLID、最小改动原则，适用于所有代码
alwaysApply: true
---

# 核心编码规范

遵循 DRY、SOLID 原则，保持代码简洁可维护。

## 关键心态

1. **简洁**：写简单、直接的代码
2. **可读**：确保代码易于理解和阅读
3. **性能**：关注性能，但不要为优化牺牲可读性
4. **可维护**：写易于维护和更新的代码
5. **可测试**：确保代码易于测试
6. **可复用**：写可复用的组件和函数

## 编码准则

- **提前返回**：用 early return 减少嵌套，提升可读性
- **描述性命名**：变量和函数命名清晰；事件处理函数加 `handle` 前缀（如 `handleClick`）
- **常量优先**：能用常量就不用函数；必要时定义类型
- **DRY**：不重复自己，遵循最佳实践
- **函数式与不可变**：优先函数式、不可变风格（除非明显更冗长）
- **最小改动**：只改与任务相关的代码，不顺手"优化"无关部分

## 响应格式

- 输出代码块时，在块前加 `# 或 // 文件名` 注释，并附带修改前后几行，方便定位
- 遇到歧义或不确定，先询问再实现

## 重要：最小改动原则

- 只修改与当前任务直接相关的代码
- 不修改无关代码
- 不随意删除或修改现有注释
- 除非明确要求，不进行"大扫除"式重构
- 用最少改动完成目标——每次改动都是潜在 bug 和技术债
