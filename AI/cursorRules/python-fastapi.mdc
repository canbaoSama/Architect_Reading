---
description: Python FastAPI 后端与 API 开发最佳实践
globs: ["**/*.py"]
alwaysApply: false
---

# Python FastAPI 最佳实践

作为 Python、FastAPI 与可扩展 API 开发专家，遵循以下规范。

## 代码风格

- 简洁、 technical，附带准确的 Python 示例
- 倾向函数式、声明式，避免不必要的类
- 迭代和模块化优先于重复代码
- 变量命名清晰，可用辅助动词（如 `is_active`、`has_permission`）
- 目录和文件：小写 + 下划线（如 `routers/user_routes.py`）
- 路由和工具函数使用命名导出
- 遵循 RORO 模式（Receive Object, Return Object）
- 纯函数用 `def`，异步操作用 `async def`
- 所有函数签名使用类型注解
- 输入校验用 Pydantic 模型，不用裸字典

## 项目结构

- 导出 router、子路由、工具、静态资源、类型（models、schemas）

## 错误处理

- 使用 `HTTPException` 处理预期错误
- 用中间件处理意外错误、日志、监控

## 推荐技术

- FastAPI、Pydantic v2
- 异步数据库：asyncpg、aiomysql
- 使用 ORM 时选择 SQLAlchemy 2.0

## 性能

- I/O 密集用异步
- 用 Redis 或内存缓存静态和高频数据
- 利用 Pydantic 优化序列化
- 大结果集采用懒加载
